import * as React from 'react';
import logo from './logo.svg';
import './App.css';
import {getFavorites} from'./data/Favorites'

export const App:React.FC=() =>{

  return (
<>
</>
  );
}
getFavorites().then(pikeman=>{

})

export default App;
